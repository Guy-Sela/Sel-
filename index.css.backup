:root {
    --bg-color: #0a0a0a;
    --text-color: #e0e0e0;
    --accent-color: #ffffff;
    --font-serif: 'Cormorant Garamond', serif;
    --font-sans: 'Inter', sans-serif;
    --spacing-unit: 2rem;
    --header-height: 80px;
    --transition-speed: 0.4s;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    scroll-padding-top: var(--header-height);
}

body {
    background-color: var(--bg-color);
    color: var(--text-color);
    font-family: var(--font-sans);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}

/* Typography */
h1,
h2,
h3,
.logo {
    font-family: var(--font-serif);
    font-weight: 400;
    color: var(--accent-color);
}

h1 {
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
}

h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: calc(var(--spacing-unit) * 2);
    text-align: center;
    font-weight: 400;
}

h3 {
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
}

p {
    font-size: 1rem;
    margin-bottom: 1.5rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.8);
}

.tagline {
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 2rem;
    display: block;
}

a {
    color: inherit;
    text-decoration: none;
    transition: opacity 0.3s ease;
}

a:hover {
    opacity: 0.7;
}

/* Header */
header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100;
    background-color: transparent;
    /* Changed from dark bg to transparent */
    /* backdrop-filter: blur(10px); Removed blur to show video underneath */
    transition: transform var(--transition-speed) ease, background-color 0.4s ease;
    border-bottom: none;
}

header.scrolled {
    background-color: rgba(10, 10, 10, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

header.header-hidden {
    transform: translateY(-100%);
}

.header-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 var(--spacing-unit);
    height: var(--header-height);
    max-width: 1400px;
    margin: 0 auto;
}

.logo-container {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    line-height: 1;
}

.logo {
    font-size: 1.8rem;
    letter-spacing: 0.05em;
    font-weight: 400;
    margin-bottom: 4px;
}

.tagline-header {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: rgba(255, 255, 255, 0.5);
}

nav ul {
    display: flex;
    list-style: none;
    gap: var(--spacing-unit);
}

nav a {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-weight: 300;
}

/* Layout & Breathing Room */
main {
    padding-top: 0;
}

section {
    padding: calc(var(--spacing-unit) * 4) var(--spacing-unit);
    min-height: 80vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
}

/* Hero */
#hero {
    height: 100vh;
    align-items: center;
    text-align: center;
    position: relative;
    overflow: hidden;
}

#hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    /* Ensure iframes don't capture mouse events */
}

.hero-media {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
    opacity: 0;
    transition: opacity 2s ease-in-out;
    object-fit: cover;
}

.hero-media.active {
    opacity: 0.3;
    /* Subtle opacity */
}

.hero-content {
    position: relative;
    z-index: 1;
}

/* Works */
.project {
    margin-bottom: calc(var(--spacing-unit) * 6);
    text-align: center;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: var(--spacing-unit);
}

.project:last-child {
    margin-bottom: 0;
}

.project-content {
    max-width: 600px;
    margin: 0 auto;
}

.project-link {
    display: inline-block;
    margin-top: 1rem;
    font-family: var(--font-sans);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-bottom: 1px solid var(--accent-color);
    padding-bottom: 2px;
}

/* About & Contact */
.about-content,
.contact-links {
    text-align: center;
}

.contact-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
}

.contact-links a {
    font-size: 1.2rem;
    font-family: var(--font-serif);
}

/* Footer */
footer {
    padding: var(--spacing-unit);
    text-align: center;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.4);
    margin-top: calc(var(--spacing-unit) * 4);
    padding-bottom: calc(var(--spacing-unit) * 2);
}

/* Mobile adjustments */
@media (max-width: 768px) {
    :root {
        --spacing-unit: 1.5rem;
    }

    .header-inner {
        padding: 0 1rem;
    }

    .logo {
        font-size: 1.5rem;
    }

    .tagline-header {
        font-size: 0.6rem;
        letter-spacing: 0.1em;
    }

    nav ul {
        gap: 1rem;
    }

    nav a {
        font-size: 0.8rem;
    }

    section {
        padding: calc(var(--spacing-unit) * 3) var(--spacing-unit);
    }
}